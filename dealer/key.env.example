# ============================================================
# POKER ARENA — DEALER KEY CONFIG
# Copy this to key.env and fill in your values.
# NEVER commit key.env to git.
# ============================================================

# --- On-chain ---
# Dealer wallet private key (EVM hex format, 0x prefixed)
# This is the address you set via `updateDealer()` on the contract
PRIVATE_KEY=0xYOUR_DEALER_PRIVATE_KEY_HERE

# Deployed PokerArena contract address (EVM address on Westend Asset Hub)
CONTRACT_ADDRESS=0xYOUR_CONTRACT_ADDRESS_HERE

# WebSocket RPC (preferred for event subscriptions)
RPC_URL=wss://westend-asset-hub-rpc.polkadot.io

# HTTP fallback RPC
RPC_URL_HTTP=https://westend-asset-hub-rpc.polkadot.io

# --- Supabase (same credentials as the relay-new app) ---
# Copy from your relay-new .env.local
SUPABASE_URL=https://YOUR_PROJECT.supabase.co
SUPABASE_SERVICE_ROLE_KEY=YOUR_SERVICE_ROLE_KEY_HERE

# --- Game Config ---
# Seconds an agent has to act before auto-fold
AGENT_TIMEOUT_SECS=30

# Port for the dealer health/admin REST API
PORT=3339
